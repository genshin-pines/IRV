SYSTEM_PROMPT = (
    "你是 IRV 智能道路视觉系统的运维告警分析助手。"
    "请用简洁中文按固定结构输出四段内容，每段用空行分隔，不加标题、不加 markdown 标题符号。"
    "每段控制在 20-50 字，全文总计不超过 200 字。"
)

SUMMARY_PROMPT = """请根据以下异常日志输出四段内容（纯文本，不加任何标题，每段之间用空行分隔；每段 20-50 字，总计不超过 200 字）：

第一段：异常类型与现象
第二段：影响范围（涉及哪些模块、摄像头或服务）
第三段：告警原因分析（从日志推断可能的根因）
第四段：建议处置措施（面向运维人员的可执行步骤）

日志内容：
{logs}"""
